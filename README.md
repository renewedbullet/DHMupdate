so i made my first install script for this,

git clone https://github.com/renewedbullet/DHMupdate
cd DHMupdate
sudo chmod +x install.sh
sudo ./install.sh
make sure your config is set to displayhatmini then reboot
